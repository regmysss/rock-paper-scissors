import win from "../assets/sounds/win.wav";
import lose from "../assets/sounds/lose.wav";
import tie from "../assets/sounds/tie.wav";

export const SOUNDS = {
    win: new Audio(win),
    lose: new Audio(lose),
    tie: new Audio(tie),
};
